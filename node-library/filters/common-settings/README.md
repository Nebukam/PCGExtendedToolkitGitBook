---
icon: sliders
---

# Common Settings

**Shared configuration blocks used across filter nodes.** The base filter definition — priority, error handling, and failure policies — is documented here, along with filter result output settings for writing pass/fail outcomes to point attributes. Individual filter pages reference these instead of repeating them.
