---
icon: sliders
---

# Common Settings

**Shared configuration blocks used across sampling nodes.** The Apply Sampling Details struct — which controls how sampled values are written back to point attributes — is documented here. Individual sampling node pages reference it instead of repeating the settings.
