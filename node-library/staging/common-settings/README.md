---
icon: sliders
---

# Common Settings

**Shared configuration blocks used across staging nodes.** Socket output details — controlling how socket transforms and metadata are written to points after asset placement — are documented here. Individual staging node pages reference these instead of repeating them.
